<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
    <title>Document</title>
    <!-- feather-icons CDN -->
    <script src="https://unpkg.com/feather-icons"></script>
    <style>
      .logo {
        cursor: pointer;
        user-select: none;
      }

      
    </style>
  </head>
  <body>
    <header class="header">
      <nav class="navbar">
        <% if(typeof link !== 'undefined' && link) { %>
        <div class="menu-icon" onclick="toPage('<%= link %>')">
          <i data-feather="chevron-left"></i>
        </div>
        <% } else { %>
        <div class="menu-icon" onclick="history.back()">
          <i data-feather="chevron-left"></i>
        </div>
        <% } %>

        <div class="close-menu" onclick="toggleSidebar()">
          <i data-feather="x"></i>
        </div>

        <% if(typeof title !== 'undefined' && title) { %>
        <div class="logo"><%= title %></div>
        <% } else { %>
        <div class="logo" onclick="toPage('/Home')">하이스쿨</div>
        <% } %>

        <div class="menu-bar">
          <a href="/home"><div>홈</div></a>
          <a href="/publicPostMain"><div>자유게시판</div></a>
          <a href="/mypageMain"><div>마이페이지</div></a>
          <a href="#"><div>고객센터</div></a>
        </div>
        <div class="nav-btn">
          <a href="/register">회원가입</a>
          <a href="/login">로그인</a>
        </div>
        <div class="settings-icon">
          <i data-feather="send"></i>
        </div>
      </nav>
    </header>
    <div id="sidebar" class="sidebar">
      <!-- <div  onclick="toggleSidebar()">
      <i data-feather="menu"></i></div> -->
      <a href="/home">홈</a>
      <a href="/publicPostMain">전체게시판</a>
      <a href="/mypageMain">마이페이지</a>
      <a href="#">고객센터</a>

      <!-- 추가적인 관리 항목 링크들 -->
    </div>

    <script>
      function toggleSidebar() {
        document.getElementById("sidebar").classList.toggle("active");
        document.querySelector(".close-menu").classList.toggle("visible");
        document.querySelector(".menu-icon").classList.toggle("hidden");
      }
      feather.replace();
    </script>

    <script>
      function toPage(link) {
        document.location.href = link;
      }
    </script>
  </body>
</html>
